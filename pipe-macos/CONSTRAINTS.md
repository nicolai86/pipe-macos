# Constraints 

## Rules that always must be true

- the ModelLoader and GCodeGenerator can never use hardcoded values; they must work purely based on the dynamic content of the step file 
- the ModelLoader and GCodeGenerator must work for round hollow stock (HSS-O), as well as rectangular hollow stock (HSS).
